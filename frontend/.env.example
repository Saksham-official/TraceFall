# Public backend origin only. This value is embedded in the browser bundle, so it must
# never contain database, Redis, blockchain-provider, or signing credentials.
# Leave empty when the SPA is served behind the repository's nginx proxy.
VITE_API_URL=https://api.example.com
